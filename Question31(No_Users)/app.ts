let usernames:string[]=[]

    if(usernames.length == 0){
        console.log("we need to add some users")
}
//Adding some users
usernames.splice(0,0,"Waheed","Irfan","Uzair")
console.log(usernames)
// Removing all users from the list
usernames.splice(0,3)
console.log(usernames)
console.log("We need to add some users")
